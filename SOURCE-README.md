# Plaifa Engineering Website Source

เว็บไซต์ React + Vite + Tailwind สำหรับ Plaifa Engineering Co.,Ltd.

## Run locally

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm check
pnpm build
```

## Assets

ภาพต้นฉบับอยู่ในโฟลเดอร์ `assets/` สำหรับนำไปโฮสต์หรืออัปโหลดกับผู้ให้บริการของคุณเอง

ไฟล์ `client/src/pages/Home.tsx` ถูกเตรียมให้ใช้ assets ภายในแพ็กผ่าน path `/assets/...` เรียบร้อยแล้ว
